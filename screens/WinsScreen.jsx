import { StyleSheet, View } from 'react-native';

function WinsScreen({ route }) {
  return (
    <View style={styles.container}>

    </View>
  );
};

export default WinsScreen;

const styles = StyleSheet.create({
  container: {
    flex: 1
  }
});